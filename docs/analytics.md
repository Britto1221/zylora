# Analytics

The public site emits same-origin first-party events. Stored events include
tenant, site, session identifier, event type, path, referrer, campaign metadata,
and timestamp. The dashboard aggregates page views, sessions, lead submissions,
chatbot activity, and conversion data.

Do not add fingerprinting or unnecessary sensitive data. Production retention
and consent rules must follow the client's jurisdiction and privacy policy.
