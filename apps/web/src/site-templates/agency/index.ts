import { exampleWebsite } from "../shared/examples";

export const agencyTemplate = exampleWebsite(
  "agency",
  "Signal Agency",
  "Strategy and execution for ambitious businesses.",
  ["Brand strategy", "Digital campaigns", "Conversion design"],
);
