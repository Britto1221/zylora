import { exampleWebsite } from "../shared/examples";

export const generalTemplate = exampleWebsite(
  "general",
  "Oak & Main",
  "Reliable local service without the friction.",
  ["Core service", "Priority support", "Custom requests"],
);
