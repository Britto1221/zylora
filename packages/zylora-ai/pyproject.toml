[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "zylora-ai"
version = "2.0.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.9",
  "httpx>=0.27"
]

[tool.hatch.build.targets.wheel]
packages = ["zylora_ai"]
